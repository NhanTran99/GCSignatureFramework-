% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery_output.R
\name{DiscoveryOutputExporter}
\alias{DiscoveryOutputExporter}
\title{Discovery Output Exporter}
\usage{
DiscoveryOutputExporter(artifacts, report)
}
\arguments{
\item{artifacts}{A single \code{artifact_contract} object, or a list of
such objects -- the same collection that was passed to
\code{\link[=DiscoveryOutputVerifier]{DiscoveryOutputVerifier()}} to produce \code{report}.}

\item{report}{The \code{verification_report} object returned by
\code{\link[=DiscoveryOutputVerifier]{DiscoveryOutputVerifier()}} for \code{artifacts}.}
}
\value{
The verified artifact collection, exactly as received (same
list structure as \code{report$artifacts}).
}
\description{
The canonical, single export component for the Discovery Output
Boundary Layer. Accepts a collection of Discovery contractual
artifacts together with the \code{VerificationReport} produced for that
exact collection by \code{\link[=DiscoveryOutputVerifier]{DiscoveryOutputVerifier()}}, and -- only if
verification passed -- returns the original artifact collection
completely unchanged (Output Immutability Principle). The
\code{VerificationReport} itself never crosses the boundary; only the
verified artifacts are returned.
}
\details{
Export is blocked (an error is raised) if verification did not pass,
or if \code{artifacts} does not match the exact collection recorded in
\code{report} (preventing a verified report from being reused to export a
different, unverified artifact collection).

No artifact relabeling, merging, splitting, or wrapper object is
introduced; the exporter preserves the original artifact collection
exactly as received.
}
