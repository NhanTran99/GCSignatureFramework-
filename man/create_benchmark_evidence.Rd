% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{create_benchmark_evidence}
\alias{create_benchmark_evidence}
\title{Create Benchmark Evidence}
\usage{
create_benchmark_evidence(
  benchmark_object,
  producer = NULL,
  consumer = "candidate_selection",
  metadata = list()
)
}
\arguments{
\item{benchmark_object}{A \code{benchmark_object}, typically the updated
object produced by \code{\link[=run_benchmark]{run_benchmark()}}.}

\item{producer}{Character scalar identifying the producer. Defaults
to the embedded Validation Evidence's \code{producer}.}

\item{consumer}{Character scalar identifying the intended downstream
consumer. Defaults to \code{"candidate_selection"} (Module 11, IDS-004).}

\item{metadata}{Additional list of metadata merged into the
artifact's \code{metadata} field. Defaults to an empty list. Must not
contain \code{framework_layer}, \code{artifact_role}, or \code{benchmark_object}
(set internally), nor \code{candidate_selection}, \code{validated_candidate},
\code{final_recommendation}, or \code{model_approval} (explicitly forbidden
content).}
}
\value{
An object of class \code{c("benchmark_evidence", "artifact_contract", "contract")}.
}
\description{
Constructs Benchmark Evidence: the sole Framework artifact produced
by Module 10 (via the LOCKED Module 02 \code{create_artifact_contract()}),
structurally independent from the supplied Benchmark Object while
preserving complete lineage by embedding it unchanged. Benchmark
Evidence never contains Candidate Selection, Validated Candidate,
final recommendation, or model approval information.
}
