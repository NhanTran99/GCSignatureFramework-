% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical_translation.R
\name{run_clinical_translation}
\alias{run_clinical_translation}
\title{Run Clinical Translation (Workflow + Executor)}
\usage{
run_clinical_translation(clinical_translation_object, translator = NULL, ...)
}
\arguments{
\item{clinical_translation_object}{A \code{clinical_translation_object},
as returned by \code{\link[=create_clinical_translation_object]{create_clinical_translation_object()}}.}

\item{translator}{A function taking \verb{(clinical_translation_object, ...)}
and returning a named list of translation components. Defaults to
an internal generic, structural placeholder translator.}

\item{...}{Additional arguments forwarded to \code{translator}.}
}
\value{
A list with elements \code{clinical_translation_object} (the
updated object, with \code{translation_components} populated) and
\code{clinical_translation_evidence} (the resulting Framework
artifact).
}
\description{
Orchestrates the Clinical Translation workflow for an initial
Clinical Translation Object: invokes a translator (the default
generic translator, or a caller-supplied one) to produce one or more
named \code{translation_components}, returns an updated Clinical
Translation Object carrying those components, and constructs the
resulting Clinical Translation Evidence -- mirroring Module 09's
\code{run_validation()} through Module 13's \code{run_evidence_integration()}.
}
\details{
The translator replaces only the execution step; it never bypasses
workflow, verification, lineage, or artifact construction performed
by this function. The default translator performs only a generic,
structural check -- no treatment recommendation, clinical guideline
reasoning, patient stratification, therapeutic reasoning, or
clinician-facing output is performed by this function or its
default translator.
}
