% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery_processing.R
\name{generic_normalization_stage}
\alias{generic_normalization_stage}
\title{Generic Normalization stage}
\usage{
generic_normalization_stage(dataset, method = c("identity", "scale"), ...)
}
\arguments{
\item{dataset}{The dataset to normalize (e.g. \code{data.frame} or
\code{matrix} of numeric values).}

\item{method}{Character scalar, one of \code{"identity"} (default) or
\code{"scale"}.}

\item{...}{Currently unused; present for interface consistency with
the \code{process(dataset, ...)} stage interface.}
}
\value{
The normalized dataset. For \code{method = "identity"}, this is
the original dataset (returned as-is, not mutated). For
\code{method = "scale"}, a new, centered-and-scaled \code{matrix} (via
\code{\link[=scale]{scale()}}).
}
\description{
A generic, reusable Normalization processing stage. Supports two
domain-agnostic modes: \code{"identity"} (the default; returns the
dataset unchanged) and \code{"scale"} (applies generic centering/scaling
via base R \code{\link[=scale]{scale()}} to numeric matrix-like data). No statistical
modelling, feature selection, or biological assumption is performed.
The input \code{dataset} is never modified in place.
}
