% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{create_validation_evidence}
\alias{create_validation_evidence}
\title{Create Validation Evidence}
\usage{
create_validation_evidence(
  validation_object,
  producer = NULL,
  consumer = "benchmark",
  metadata = list()
)
}
\arguments{
\item{validation_object}{A \code{validation_object}, typically the
updated object produced by \code{\link[=run_validation]{run_validation()}}.}

\item{producer}{Character scalar identifying the producer. Defaults
to the embedded Model Candidate's \code{producer}.}

\item{consumer}{Character scalar identifying the intended downstream
consumer. Defaults to \code{"benchmark"} (Module 10, IDS-004).}

\item{metadata}{Additional list of metadata merged into the
artifact's \code{metadata} field. Defaults to an empty list. Must not
contain \code{framework_layer}, \code{artifact_role}, or \code{validation_object}
(set internally), nor \code{benchmark_evidence}, \code{candidate_selection},
or \code{validated_candidate} (explicitly forbidden content).}
}
\value{
An object of class \code{c("validation_evidence", "artifact_contract", "contract")}.
}
\description{
Constructs Validation Evidence: the sole Framework artifact produced
by Module 09 (via the LOCKED Module 02 \code{create_artifact_contract()}),
structurally independent from the supplied Validation Object while
preserving complete lineage by embedding it unchanged. Validation
Evidence never contains Benchmark Evidence, Candidate Selection, or
Validated Candidate information.
}
