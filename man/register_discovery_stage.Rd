% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery_pipeline.R
\name{register_discovery_stage}
\alias{register_discovery_stage}
\title{Register a Discovery Pipeline Stage}
\usage{
register_discovery_stage(
  pipeline,
  stage_name,
  inputs = character(),
  outputs = character(),
  dependencies = character(),
  executor
)
}
\arguments{
\item{pipeline}{A \code{discovery_pipeline} object, as returned by
\code{\link[=create_discovery_pipeline]{create_discovery_pipeline()}}.}

\item{stage_name}{Character scalar. A unique name for the stage
within the pipeline.}

\item{inputs}{Character vector of abstract input names required by
this stage. Defaults to \code{character()}.}

\item{outputs}{Character vector of abstract output names produced
by this stage. Defaults to \code{character()}.}

\item{dependencies}{Character vector of \code{stage_name}s that this
stage depends on. Defaults to \code{character()}.}

\item{executor}{A function serving as a placeholder for future
algorithm execution. This module never invokes it; no algorithm
may be attached here.}
}
\value{
The updated \code{discovery_pipeline} object, with the new stage
added and \code{status} set to \code{"stages_registered"}.
}
\description{
Adds a stage to a Discovery Pipeline. A stage is a named
orchestration unit declaring its abstract inputs, outputs, and
dependencies on other stages. The \code{executor} is a placeholder only;
no executable algorithm is attached or invoked by this function or
by this module.
}
