% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery_pipeline.R
\name{run_discovery_pipeline}
\alias{run_discovery_pipeline}
\title{Run a Discovery Pipeline (Orchestration Only)}
\usage{
run_discovery_pipeline(pipeline)
}
\arguments{
\item{pipeline}{A \code{discovery_pipeline} object.}
}
\value{
The updated \code{discovery_pipeline} object, with \code{status} set
to \code{"executed"} and \code{metadata$execution_order} set to the
resolved stage execution order (character vector of stage names).
}
\description{
Performs an orchestration-only "run" of a Discovery Pipeline:
validates the pipeline, resolves a valid stage execution order, and
records that order on the pipeline's metadata and status. This
function does \strong{not} invoke any stage's \code{executor}, and performs no
computation of any kind; the placeholder executors registered via
\code{\link[=register_discovery_stage]{register_discovery_stage()}} are never called by this module.
}
