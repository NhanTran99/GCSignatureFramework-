% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_output.R
\name{create_model_candidate}
\alias{create_model_candidate}
\title{Create a Model Candidate}
\usage{
create_model_candidate(
  training_object,
  producer = NULL,
  consumer = "validation_benchmark",
  metadata = list()
)
}
\arguments{
\item{training_object}{A \code{training_object} (Module 07), as returned
by \code{create_training_object()}.}

\item{producer}{Character scalar identifying the producer. Defaults
to the embedded Model Object's \code{algorithm_id}.}

\item{consumer}{Character scalar identifying the intended downstream
consumer. Defaults to \code{"validation_benchmark"} (IDS-004).}

\item{metadata}{Additional list of metadata merged into the
artifact's \code{metadata} field. Defaults to an empty list. Must not
contain \code{framework_layer}, \code{artifact_role}, or \code{training_object}
(these are set internally).}
}
\value{
An object of class \code{c("model_candidate", "artifact_contract", "contract")}.
}
\description{
Constructs a Model Candidate: a Framework artifact (via the LOCKED
Module 02 \code{create_artifact_contract()}) that preserves complete
Modeling lineage by embedding the complete, unmodified Training
Object (Module 07). Module 08 is the only Coding Module permitted to
create Model Candidates (AD-01).
}
\details{
The supplied Training Object is consumed read-only and is never
modified (AD-03); \code{trained_model} within it is treated as an opaque
implementation object and is never inspected or transformed (Opaque
Implementation Principle).
}
