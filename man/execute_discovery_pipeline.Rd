% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery_processing.R
\name{execute_discovery_pipeline}
\alias{execute_discovery_pipeline}
\title{Execute a Discovery Pipeline (Execution Layer)}
\usage{
execute_discovery_pipeline(
  pipeline,
  dataset,
  producer = NULL,
  consumer = "discovery_downstream",
  ...
)
}
\arguments{
\item{pipeline}{A \code{discovery_pipeline} object (see
\code{\link[=create_discovery_pipeline]{create_discovery_pipeline()}} and \code{\link[=register_discovery_stage]{register_discovery_stage()}}).}

\item{dataset}{The initial dataset to process (e.g. a \code{data.frame},
\code{matrix}, or, if available, a \code{SummarizedExperiment}). Never
modified in place.}

\item{producer}{Character scalar identifying the producer for the
resulting Discovery contractual artifact. Defaults to the
pipeline's \code{pipeline_name}.}

\item{consumer}{Character scalar identifying the intended consumer
for the resulting Discovery contractual artifact (e.g. a
downstream Modeling pipeline name). Defaults to
\code{"discovery_downstream"}.}

\item{...}{Additional arguments forwarded to every stage executor.}
}
\value{
A list with three elements:
\describe{
\item{processed_dataset}{The final dataset after all stages
have executed, in resolved order.}
\item{artifact}{An \code{artifact_contract} object (per Module 02),
with \code{metadata$framework_layer = "processing"}, recording this
execution as a Discovery contractual artifact.}
\item{execution_order}{Character vector giving the stage
execution order that was used.}
}
}
\description{
Actually executes a Discovery Pipeline's registered stages, in
dependency-resolved order, against a single dataset that is threaded
sequentially through each stage's \code{executor}. This is the Execution
Layer introduced by Module 04, kept strictly separate from Module
03's orchestration-only \code{run_discovery_pipeline()} (which never
invokes executors). This function internally reuses
\code{\link[=validate_discovery_pipeline]{validate_discovery_pipeline()}} and \code{\link[=resolve_pipeline_dependencies]{resolve_pipeline_dependencies()}}
as-is; it does not duplicate any orchestration logic.
}
\details{
Per the Immutable Dataset Principle, the \code{dataset} argument passed
in by the caller is never modified in place: each stage executor is
expected to return a new processed dataset (per the
\code{process(dataset, ...) -> processed_dataset} interface), and this
function only ever rebinds a local variable to thread the data
through stages -- the object the caller passed in is left untouched.
}
