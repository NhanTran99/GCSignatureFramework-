% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery_processing.R
\name{generic_filtering_stage}
\alias{generic_filtering_stage}
\title{Generic Filtering stage}
\usage{
generic_filtering_stage(dataset, keep_rows = NULL, keep_cols = NULL, ...)
}
\arguments{
\item{dataset}{The dataset to filter (e.g. \code{data.frame} or
\code{matrix}).}

\item{keep_rows}{Optional logical or integer vector selecting which
rows to keep. Defaults to \code{NULL} (keep all rows).}

\item{keep_cols}{Optional logical or integer vector selecting which
columns to keep. Defaults to \code{NULL} (keep all columns).}

\item{...}{Currently unused; present for interface consistency with
the \code{process(dataset, ...)} stage interface.}
}
\value{
A new, filtered dataset of the same class as \code{dataset}.
}
\description{
A generic, reusable Filtering processing stage. Performs only
generic row/column subsetting based on explicitly supplied indices
or a logical/integer selection -- no statistical or biological
criterion is applied internally. When neither \code{keep_rows} nor
\code{keep_cols} is supplied, the dataset is returned unchanged (identity
filtering). The input \code{dataset} is never modified in place.
}
