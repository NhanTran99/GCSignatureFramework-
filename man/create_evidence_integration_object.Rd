% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evidence_integration.R
\name{create_evidence_integration_object}
\alias{create_evidence_integration_object}
\title{Create an Evidence Integration Object}
\usage{
create_evidence_integration_object(interpretation_evidence, metadata = list())
}
\arguments{
\item{interpretation_evidence}{An \code{interpretation_evidence} object
(Module 12).}

\item{metadata}{List of additional implementation metadata.
Defaults to an empty list.}
}
\value{
An object of class \code{"evidence_integration_object"}, with
fields \code{interpretation_evidence}, \code{integration_components}
(initially an empty list), \code{execution_metadata} (initially an
empty list), and \code{metadata}.
}
\description{
Constructs an Evidence Integration Object: an implementation-only
object (never exposed as a Framework artifact) that encapsulates
the consumed Interpretation Evidence (Module 12), integration
components, execution metadata, and implementation state.
Interpretation Evidence is consumed completely read-only and is
never modified by this function or by \code{\link[=run_evidence_integration]{run_evidence_integration()}}.
}
\details{
Only an \code{interpretation_evidence} object is accepted as the
upstream contract; this module never directly consumes Validated
Candidate, Benchmark Evidence, or Validation Evidence -- those
remain available, if needed, only through the lineage already
preserved within the Interpretation Evidence.
}
