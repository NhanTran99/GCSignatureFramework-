% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_output.R
\name{create_modeling_evidence}
\alias{create_modeling_evidence}
\title{Create Modeling Evidence}
\usage{
create_modeling_evidence(
  training_object,
  producer = NULL,
  consumer = "validation_benchmark",
  metadata = list()
)
}
\arguments{
\item{training_object}{A \code{training_object} (Module 07), as returned
by \code{create_training_object()}.}

\item{producer}{Character scalar identifying the producer. Defaults
to the embedded Model Object's \code{algorithm_id}.}

\item{consumer}{Character scalar identifying the intended downstream
consumer. Defaults to \code{"validation_benchmark"} (IDS-004).}

\item{metadata}{Additional list of metadata merged into the
artifact's \code{metadata} field. Defaults to an empty list. Must not
contain \code{framework_layer}, \code{artifact_role}, \code{training_object}, or
\code{evidence_sections} (these are set internally).}
}
\value{
An object of class \code{c("modeling_evidence", "artifact_contract", "contract")}.
}
\description{
Constructs Modeling Evidence: a Framework artifact (via the LOCKED
Module 02 \code{create_artifact_contract()}) acting as a structural
evidence container that accompanies exactly one Model Candidate.
Module 08 is the only Coding Module permitted to create Modeling
Evidence (AD-02).
}
\details{
The five structural sections (\code{predictive_model},
\code{risk_score_definition}, \code{performance_profile},
\code{interpretability_profile}, \code{clinical_evaluation_profile}) are
initialized as placeholder (\code{NULL}) implementation objects; no
benchmark, statistical analysis, or clinical evaluation is performed
here (those responsibilities belong to IDS-004).

Modeling Evidence does not embed the Model Candidate, and is not
embedded by it (AD-04); both independently reference the same,
unmodified Training Object for shared lineage.
}
