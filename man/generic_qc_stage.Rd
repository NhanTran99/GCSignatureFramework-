% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery_processing.R
\name{generic_qc_stage}
\alias{generic_qc_stage}
\title{Generic Quality Control stage}
\usage{
generic_qc_stage(dataset, ...)
}
\arguments{
\item{dataset}{The dataset to inspect (e.g. \code{data.frame}, \code{matrix},
or, if available, \code{SummarizedExperiment}).}

\item{...}{Currently unused; present for interface consistency with
the \code{process(dataset, ...)} stage interface.}
}
\value{
The dataset, structurally unchanged, with a \code{"qc_info"}
attribute attached (a list with \code{dim} and, where supported,
\code{n_missing}).
}
\description{
A generic, reusable Quality Control processing stage. Performs only
structural validation (checks the dataset has two dimensions, and
that neither is zero) and a missing-value inspection (where
supported), and returns the dataset unchanged, with QC summary
information recorded as an attribute on the returned object. No
biological, statistical, or domain-specific assumption is made. The
input \code{dataset} is never modified in place; the QC information is
attached to a new object that is returned.
}
