% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_construction.R
\name{create_model_contract}
\alias{create_model_contract}
\title{Create a Model Contract}
\usage{
create_model_contract(specification, discovery_artifacts, metadata = list())
}
\arguments{
\item{specification}{A \code{model_specification} object, as returned by
\code{\link[=create_model_specification]{create_model_specification()}}.}

\item{discovery_artifacts}{A single \code{artifact_contract} object, or
a list of such objects, as verified and exported by Module 05
(e.g. via \code{DiscoveryOutputExporter()}).}

\item{metadata}{List of additional Framework metadata to record on
the contract. Defaults to an empty list.}
}
\value{
An object of class \code{"model_contract"}, with fields
\code{specification} (the supplied Model Specification, unchanged),
\code{discovery_artifacts} (the supplied Discovery artifact(s),
unchanged, always as a list), and \code{metadata}.
}
\description{
Constructs a Model Contract: an internal, implementation-only
object (NOT a new Framework Contract type, and not a modification
of the canonical Framework Contract hierarchy established in Module
02) that preserves Model Specification lineage, Discovery artifact
lineage, and Framework metadata. Model Contract remains internal to
the Modeling implementation and is never exported across any
domain boundary.
}
\details{
The supplied Discovery artifact(s) are consumed read-only: they are
verified to be \code{artifact_contract} objects (per Module 02) and are
otherwise preserved completely unchanged -- this function never
reinterprets, modifies, or generates a Discovery artifact.
}
